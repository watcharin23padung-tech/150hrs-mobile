# คู่มือเผยแพร่แอพขึ้นเว็บ (สำหรับคนที่ไม่เคยเขียนโค้ดมาก่อน)

คู่มือนี้จะพาทำทีละคลิก ไม่ต้องเข้าใจโค้ดเลยก็ทำตามได้ ใช้เวลารวมประมาณ 20-30 นาที

**สิ่งที่จะได้ในตอนท้าย**: เว็บแอพที่มีลิงก์ให้คนอื่นเปิดใช้งานได้จริง และทุกครั้งที่คุณ (หรือทีม) แก้ไขโค้ดแล้วอัปขึ้นไปใหม่ เว็บจะอัปเดตให้เองอัตโนมัติ ไม่ต้องทำอะไรเพิ่ม

**อุปกรณ์ที่ต้องมี**: คอมพิวเตอร์ (Windows หรือ Mac), อินเทอร์เน็ต, ไฟล์ `150hrs-mobile-app.zip` ที่ได้รับไปแล้ว

---

## ทำความเข้าใจก่อนเริ่ม (30 วินาที)

จะมี "บ้าน" 3 หลังที่ต้องเชื่อมกัน:

- **GitHub** = ที่เก็บ "ต้นฉบับ" ของโค้ดแอพ เหมือน Google Drive สำหรับโค้ด
- **Vercel** = "โรงงาน" ที่หยิบโค้ดจาก GitHub มาแปลงเป็นเว็บไซต์ที่คนเปิดดูได้ แล้วก็คอยเช็คอัตโนมัติว่ามีโค้ดใหม่ไหม ถ้ามีก็อัปเดตเว็บให้เอง
- **Supabase** = ฐานข้อมูล เก็บข้อมูลผู้ใช้และบันทึกชั่วโมงต่างๆ (ส่วนนี้ทำเสร็จเรียบร้อยแล้ว ไม่ต้องทำอะไรเพิ่ม)

สิ่งที่คุณต้องทำคือ: เอาโค้ดขึ้น GitHub → บอก Vercel ว่า "ไปหยิบโค้ดจากตรงนี้นะ" → เสร็จ

---

## ส่วนที่ 1: ติดตั้งโปรแกรมที่จำเป็น

### 1.1 ติดตั้ง Node.js

Node.js คือโปรแกรมที่ทำให้คอมพิวเตอร์ "รัน" โค้ดของแอพนี้ได้

1. เปิดเบราว์เซอร์ไปที่ **nodejs.org**
2. จะเห็นปุ่มดาวน์โหลด 2 ปุ่ม ให้กดปุ่มที่เขียนว่า **LTS** (ปุ่มซ้าย เป็นเวอร์ชันเสถียร)
3. เปิดไฟล์ที่ดาวน์โหลดมา แล้วกด "Next" ไปเรื่อยๆ จนติดตั้งเสร็จ (ใช้ค่าเริ่มต้นทั้งหมดได้เลย ไม่ต้องปรับอะไร)
4. รีสตาร์ทคอมพิวเตอร์ 1 ครั้งหลังติดตั้งเสร็จ (เพื่อความชัวร์)

**ทดสอบว่าติดตั้งสำเร็จ**: เปิดโปรแกรม Terminal
- **Windows**: กดปุ่ม Start พิมพ์คำว่า "PowerShell" แล้วกด Enter
- **Mac**: กด Command+Space พิมพ์คำว่า "Terminal" แล้วกด Enter

จะเห็นหน้าจอสีดำๆ มีเคอร์เซอร์กระพริบ ให้พิมพ์คำสั่งนี้แล้วกด Enter:
```
node -v
```
ถ้าขึ้นตัวเลขเวอร์ชัน เช่น `v20.11.0` แปลว่าติดตั้งสำเร็จแล้ว ให้เก็บหน้าต่าง Terminal นี้ไว้ใช้ต่อ

### 1.2 ติดตั้ง Git

Git คือเครื่องมือที่ใช้ส่งโค้ดขึ้น GitHub

1. ไปที่ **git-scm.com/downloads**
2. เลือกระบบปฏิบัติการของคุณ (Windows/Mac) แล้วดาวน์โหลด
3. เปิดไฟล์ที่ดาวน์โหลดมา กด "Next" ไปเรื่อยๆ ใช้ค่าเริ่มต้นทั้งหมดได้เลย

**ทดสอบ**: กลับไปที่ Terminal พิมพ์:
```
git -v
```
ถ้าขึ้นตัวเลขเวอร์ชันแปลว่าสำเร็จ

---

## ส่วนที่ 2: เตรียมไฟล์โค้ด

1. หาไฟล์ **150hrs-mobile-app.zip** ที่ดาวน์โหลดไว้ (ปกติจะอยู่ในโฟลเดอร์ Downloads)
2. คลิกขวาที่ไฟล์ → เลือก **"Extract All..."** (Windows) หรือดับเบิลคลิก (Mac) เพื่อแตกไฟล์ออกมา
3. จะได้โฟลเดอร์ชื่อ `150hrs-app` ให้ย้ายโฟลเดอร์นี้ไปวางที่จำง่ายๆ เช่น หน้า Desktop

---

## ส่วนที่ 3: สมัครและตั้งค่า GitHub

GitHub คือที่เก็บโค้ด เหมือนบัญชี Google Drive แต่สำหรับโค้ดโดยเฉพาะ

### 3.1 สมัครบัญชี

1. ไปที่ **github.com**
2. กด **Sign up** มุมขวาบน
3. กรอกอีเมล ตั้งรหัสผ่าน ตั้งชื่อบัญชี (username) — ใช้ชื่อที่จำง่าย เป็นภาษาอังกฤษ ไม่มีเว้นวรรค
4. ทำตามขั้นตอนยืนยันตัวตนจนเสร็จ (จะมีอีเมลยืนยันส่งไปให้กดยืนยัน)

### 3.2 สร้างที่เก็บโค้ด (Repository)

1. หลัง login แล้ว กดเครื่องหมาย **+** มุมขวาบน → เลือก **New repository**
2. ช่อง "Repository name" พิมพ์ว่า `150hrs-mobile`
3. เลือก **Private** (เพื่อไม่ให้คนนอกเห็นโค้ด)
4. **สำคัญ**: อย่าติ๊กถูกช่องไหนเลยในหน้านี้ (README, .gitignore, license) ปล่อยว่างทั้งหมด
5. กดปุ่มสีเขียว **Create repository**
6. จะเห็นหน้าเปล่าๆ ที่มีคำสั่งให้คัดลอก — **ไม่ต้องคัดลอกจากตรงนี้** ให้ใช้คำสั่งในขั้นถัดไปแทน แต่ให้จำ URL ของหน้านี้ไว้ (ด้านบนจะมีข้อความคล้าย `github.com/ชื่อบัญชีของคุณ/150hrs-mobile`)

---

## ส่วนที่ 4: ส่งโค้ดขึ้น GitHub

กลับไปที่หน้าต่าง Terminal ที่เปิดค้างไว้

### 4.1 เข้าไปในโฟลเดอร์โค้ด

พิมพ์คำสั่งนี้ (ถ้าวางโฟลเดอร์ไว้ที่ Desktop):

**Windows:**
```
cd Desktop\150hrs-app
```
**Mac:**
```
cd ~/Desktop/150hrs-app
```

กด Enter — ถ้าไม่ขึ้น error อะไรแปลว่าเข้าไปในโฟลเดอร์ถูกต้องแล้ว

*เคล็ดลับ: ถ้าไม่แน่ใจว่าโฟลเดอร์อยู่ที่ไหน ให้ลากโฟลเดอร์ `150hrs-app` มาวางในหน้าต่าง Terminal หลังพิมพ์ `cd ` (มีเว้นวรรค) แล้วมันจะเติม path ให้เองอัตโนมัติ*

### 4.2 บอก Git ว่าคุณคือใคร (ทำครั้งเดียวเท่านั้น)

พิมพ์ 2 บรรทัดนี้ทีละบรรทัด (แทนด้วยอีเมล/ชื่อจริงของคุณ):
```
git config --global user.email "อีเมลของคุณ@gmail.com"
git config --global user.name "ชื่อของคุณ"
```

### 4.3 ส่งโค้ดขึ้น GitHub

พิมพ์คำสั่งต่อไปนี้ **ทีละบรรทัด** กด Enter รอให้เสร็จก่อนพิมพ์บรรทัดถัดไป:

```
git init
```
(สร้างระบบติดตามโค้ดในโฟลเดอร์นี้)

```
git add .
```
(บอกว่า "เอาไฟล์ทั้งหมดในนี้ไปเตรียมส่ง")

```
git commit -m "initial commit"
```
(เก็บสแนปช็อตของโค้ดตอนนี้ไว้)

```
git branch -M main
```
(ตั้งชื่อสายหลักของโค้ดว่า main)

```
git remote add origin https://github.com/ชื่อบัญชีของคุณ/150hrs-mobile.git
```
**สำคัญมาก**: แทนคำว่า `ชื่อบัญชีของคุณ` ด้วย username GitHub จริงของคุณ (ดูจาก URL ในขั้นตอน 3.2)

```
git push -u origin main
```
(ส่งโค้ดขึ้น GitHub จริงๆ)

**ตอนรันบรรทัดสุดท้าย** อาจมีหน้าต่างเบราว์เซอร์เด้งขึ้นมาให้ login GitHub และกด Authorize — ทำตามนั้นได้เลย

รอสักครู่ ถ้าเห็นข้อความจบด้วยคำว่า `main -> main` แปลว่าสำเร็จ

**ตรวจสอบ**: กลับไปที่หน้า GitHub repo ของคุณ กด refresh หน้าเว็บ ควรเห็นรายชื่อไฟล์ทั้งหมดของแอพขึ้นอยู่แล้ว

---

## ส่วนที่ 5: สมัครและเชื่อม Vercel

Vercel คือระบบที่จะหยิบโค้ดจาก GitHub ไปทำเป็นเว็บไซต์จริงให้อัตโนมัติ

### 5.1 สมัครบัญชี

1. ไปที่ **vercel.com**
2. กด **Sign Up**
3. เลือก **Continue with GitHub** (สำคัญ — เลือกอันนี้ เพื่อให้ Vercel เชื่อมกับ GitHub ได้อัตโนมัติ)
4. หน้าจอจะพาไป login GitHub (ถ้ายัง login ค้างอยู่จะข้ามขั้นตอนนี้ไปเลย) แล้วกด **Authorize Vercel**

### 5.2 นำเข้าโปรเจ็กต์

1. หลัง login เข้า Vercel แล้ว จะเห็นหน้า Dashboard กดปุ่ม **Add New...** (มุมขวาบน) → เลือก **Project**
2. จะเห็นรายชื่อ repository ทั้งหมดที่มีในบัญชี GitHub ของคุณ หา **150hrs-mobile**
   - ถ้าไม่เห็นในรายการ: กดลิงก์ **Adjust GitHub App Permissions** ที่อยู่ใกล้ๆ แล้วติ๊กอนุญาตให้ Vercel เข้าถึง repo `150hrs-mobile`
3. เจอแล้วให้กดปุ่ม **Import** ข้างชื่อ repo นั้น

### 5.3 ตั้งค่าก่อน deploy (ขั้นตอนสำคัญ)

หน้าที่ขึ้นมาจะมีหัวข้อ "Configure Project" — ปล่อยทุกอย่างเป็นค่าเริ่มต้น (Vercel จะรู้เองว่าเป็นแอพ Next.js) **แต่ให้ทำเพิ่ม 1 อย่าง**:

1. หาส่วนที่เขียนว่า **Environment Variables** (มักจะพับซ่อนอยู่ ให้กดคลิกที่หัวข้อนี้เพื่อขยาย)
2. จะมีช่องให้กรอก 2 ช่องคือ "Name" กับ "Value" ให้เพิ่มทีละคู่ (กดปุ่ม "Add" หลังกรอกแต่ละคู่):

   **คู่ที่ 1**
   - Name: `NEXT_PUBLIC_SUPABASE_URL`
   - Value: `https://oefjzzexjzdnqaqiwzrv.supabase.co`

   **คู่ที่ 2**
   - Name: `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - Value: `sb_publishable_5jTOSXeV1BfbqvIm426PMw_EDVuw6ec`

3. ตรวจว่าใส่ครบทั้ง 2 คู่แล้ว (ไม่มีเว้นวรรคเกินหน้า-หลัง)

### 5.4 กด Deploy

1. กดปุ่มสีดำใหญ่ **Deploy**
2. จะเห็นหน้าจอแสดงความคืบหน้า (มีข้อความวิ่งๆ คล้ายกำลังติดตั้ง) รอประมาณ 1-2 นาที
3. เมื่อเสร็จจะเห็นข้อความ **"Congratulations!"** พร้อมรูปหน้าเว็บของคุณ
4. กดปุ่ม **Visit** เพื่อเปิดเว็บจริง — ควรเห็นหน้า Login ของแอพ "150+ ชั่วโมงฝึกฯ"

**เก็บลิงก์นี้ไว้** — นี่คือเว็บแอพที่ใช้งานได้จริงแล้ว ส่งลิงก์นี้ให้นิสิต/อาจารย์เปิดใช้ได้เลย

---

## ส่วนที่ 6: ทดสอบว่าระบบอัปเดตอัตโนมัติทำงานจริง (ไม่บังคับ แต่แนะนำให้ลองสักครั้ง)

ทุกครั้งที่มีคนแก้โค้ดแล้วส่งขึ้น GitHub ใหม่ Vercel จะอัปเดตเว็บให้เองโดยไม่ต้องทำอะไรเพิ่ม ลองทดสอบดูได้ดังนี้:

1. เปิดไฟล์ใดก็ได้ในโฟลเดอร์ `150hrs-app` ด้วยโปรแกรม Notepad (Windows) หรือ TextEdit (Mac) แล้วแก้ตัวอักษรสักตัว บันทึก
2. กลับไปที่ Terminal (ต้องยังอยู่ในโฟลเดอร์ `150hrs-app`) พิมพ์ทีละบรรทัด:
   ```
   git add .
   git commit -m "ทดสอบแก้ไข"
   git push
   ```
3. กลับไปที่เว็บ Vercel → เข้าโปรเจ็กต์ `150hrs-mobile` → กดแท็บ **Deployments**
4. จะเห็นรายการใหม่ขึ้นมาเองพร้อมสถานะกำลัง Build — รอจนขึ้นคำว่า **Ready**
5. เปิดลิงก์เว็บของคุณอีกครั้ง จะเห็นการแก้ไขที่เพิ่งทำอัปเดตขึ้นไปแล้ว

---

## สรุปสั้นๆ ว่าแต่ละส่วนทำอะไร

- ทำครั้งเดียวจบ: ส่วนที่ 1-5 (ติดตั้งโปรแกรม, สมัคร GitHub/Vercel, เชื่อมกัน)
- ใช้ซ้ำได้ตลอด: ทุกครั้งที่แก้โค้ดในอนาคต ทำแค่ 3 บรรทัดนี้ในโฟลเดอร์โค้ด แล้วรอเว็บอัปเดตเองได้เลย:
  ```
  git add .
  git commit -m "อธิบายว่าแก้อะไร"
  git push
  ```

---

## ถ้าติดปัญหา

คัดลอกข้อความ error ที่ขึ้นมาทั้งหมด (สีแดงหรือมีคำว่า error) มาส่งให้ดู จะช่วยวิเคราะห์และบอกวิธีแก้ให้ทีละขั้นตอนต่อได้เลยครับ ไม่ต้องเข้าใจว่ามันแปลว่าอะไรก็ได้ แค่คัดลอกมาทั้งหมด
